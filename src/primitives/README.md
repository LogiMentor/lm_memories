# Primitive Sources

This directory is reserved for explicit vendor primitive or IP wrappers.

Wrappers added here should keep the public `lm_mem_*` interface style and
document the supported vendor, family, primitive, and tool versions.
