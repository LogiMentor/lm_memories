# Inference Sources

This directory is reserved for vendor, tool, and family-aware RAM inference
implementations.

The first import keeps runnable portable sources in `src/generic/`. Inference
modules will be added after each vendor flow has at least a synthesis smoke
test or a documented coding template.
