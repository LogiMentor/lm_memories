# Legacy Tools

`ces_mem_from_file_gen.tcl` is preserved as legacy input-file memory generation
logic. It was moved here from `legacy_inputs/` during the first public import.

Future work should migrate the supported input formats into `lm_memgen` while
keeping behaviour-compatible regression tests.
